# Supabase project URL (Project Settings > API)
SUPABASE_URL=

# Supabase service role key (Project Settings > API) - keep secret, server-side only
SUPABASE_SERVICE_KEY_ROLE=

# Origin allowed by CORS (e.g. http://localhost:5173)
ALLOWED_ORIGIN=
